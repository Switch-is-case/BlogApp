<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-white d-flex align-items-center justify-content-center vh-100">

    <div class="card p-4 shadow-lg bg-secondary text-light" style="width: 600px;">
        <h1 class="text-warning text-center"><%= post.title %></h1>
        <h5 class="text-info text-center">Автор: <%= post.author %></h5>
        <p class="post-content mt-3"><%= post.content %></p>

        <p class="text-muted small mt-2">Создано: <%= new Date(post.createdAt).toLocaleString("ru-RU") %></p>

        <div class="d-flex justify-content-between mt-4">
            <a href="/home" class="btn btn-primary">🔙 Назад</a>
            <a href="/posts/edit/<%= post._id %>" class="btn btn-warning">✏️ Редактировать</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>