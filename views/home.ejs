<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>

    <!-- Подключение Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-white">

    <div class="container mt-5">
        <h1 class="text-warning text-center mb-4">All Posts</h1>

        <div class="row">
            <% posts.forEach(post => { %>
                <div class="col-md-6">
                    <div class="card bg-secondary text-white mb-4 shadow-lg">
                        <div class="card-body">
                            <h2 class="card-title text-warning"><%= post.title %></h2>
                            <p class="card-text"><%= post.content.substring(0, 100) %>...</p>
                            <a href="/post/<%= post._id %>" class="btn btn-primary">Read more</a>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

    <!-- Подключение Bootstrap JS (необязательно, если не используются интерактивные компоненты) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>